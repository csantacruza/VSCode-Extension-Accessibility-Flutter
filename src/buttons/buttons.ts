import { Button } from './buttons.interface';

const buttons: Button[] = [
  {
    command: 'extension.accessibility.activate',
    text: 'Active accessibility UI $(flame)',
    tooltip: 'Click to run flutter accessibility in this file',
  },
];

export default buttons;
